import React from "react";
import LoginComponent from "../components/Auth/Login"; // Correcting the path based on your structure


const LoginPage = () => {
  return <LoginComponent />;
};

export default LoginPage;